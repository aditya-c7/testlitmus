# Precedent — contract playbook service

See the repo root `../README.md` for full docs.

Quick start (Windows):

```powershell
.\start.ps1
```

Then open http://127.0.0.1:8000/.

Config: copy `.env.example` → `.env` (`OPENAI_BASE_URL`, `OPENAI_API_KEY`).
No keys = demo mode (offline heuristic, still fully working).
